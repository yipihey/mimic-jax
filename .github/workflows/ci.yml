name: Mimic CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        # JAX 0.4 does not publish jaxlib wheels for Python 3.14. Pin the
        # repository's supported CI runtime instead of following the newest 3.x.
        python-version: '3.12'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libhdf5-dev libyaml-dev pkg-config

    - name: Install Python dependencies
      run: |
        pip install -r requirements.txt

    - name: Check code formatting
      run: |
        make check-format

    - name: Build Mimic
      run: |
        make clean
        make

    - name: Check property metadata is up-to-date
      run: |
        make check-generated

    - name: Validate documentation links
      run: |
        make check-docs

    - name: Validate published run reports
      run: |
        python scripts/check_reports.py

    - name: Run mimic-jax reporting and local-response tests
      env:
        JAX_ENABLE_X64: "1"
      run: |
        python -m pytest -q \
          tests/mimic_jax/test_reporting.py \
          tests/mimic_jax/test_linear_response.py \
          tests/mimic_jax/test_model_comparison_contract.py

    - name: Run SHARK Lagos23 JAX validation tests
      env:
        JAX_ENABLE_X64: "1"
      run: |
        python -m pytest -q tests/mimic_jax/test_shark_*.py

    - name: Validate module metadata
      run: |
        make validate-modules

    - name: Run unit tests
      run: |
        make tests-unit

    - name: Run integration tests
      run: |
        make tests-integration

    - name: Run scientific tests
      # The SAGE galaxy-physics baseline is generated on macOS; Linux libm/compiler
      # reproduces it only to ~7e-4 relative (real noise floor measured 6.8e-4).
      # Relax the gate above that floor for this cross-platform run. The strict
      # 1e-6 default still applies locally, and diffs in (1e-6, 1e-3] are reported
      # as warnings. The absolute floor (atol) is fixed in the comparator.
      env:
        MIMIC_BASELINE_RTOL: "1e-3"
      run: |
        make tests-scientific

    # Memory-leak gating happens inside the test suites: C unit tests call
    # check_memory_leaks() in-process, and integration tests assert
    # check_no_memory_leaks() against each Mimic run's captured output.
    # (A previous standalone step here grepped metadata/*.log files that no
    # Mimic run ever writes, so it could never fail.)

    - name: Upload test results
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: |
          tests/unit/build/*.log
          tests/data/output/binary/metadata/
